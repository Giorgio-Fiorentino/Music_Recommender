pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
jupyter>=1.0
# Interactive prototype:
streamlit>=1.30
# Matrix factorization (library SVD comparison) is OPTIONAL. scikit-surprise
# compiles C/Cython extensions and often fails to build on hosted platforms
# (e.g. Streamlit Cloud), which would fail the whole deploy. The pipeline runs
# without it (lazy import + graceful skip); the from-scratch implicit ALS is the
# primary matrix-factorization model. To enable the SVD comparison locally:
#   pip install scikit-surprise>=1.1.4
