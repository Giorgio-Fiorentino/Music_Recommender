# Virtual environment
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS
.DS_Store

# Internal design specs (not part of the submission)
docs/superpowers/

# Raw dataset (downloaded via scripts/download_data.py; not redistributed per license)
data/raw/*
!data/raw/README.md

# Processed artifacts (regenerated by the pipeline)
data/processed/*
!data/processed/README.md

# Generated results (regenerated by main.py).
# NOTE: metrics.csv and the comparison figures ARE committed so the deployed
# Streamlit "Evaluate" page works on Streamlit Cloud (where `python main.py`
# cannot be run). Re-running main.py locally simply overwrites them.
